<%= form_for(@comment) do |f| %>
  <div class="form-group">
    <%= f.hidden_field :commentable_id, :value => @link.id %>
    <%= f.label :comment %>
    <%= f.text_field :comment %>
  </div>
  <%= f.submit(:class => "btn btn-danger") %>
<% end %>


